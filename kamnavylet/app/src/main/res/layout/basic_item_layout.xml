<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/colorPrimary" >

    <ImageView
        android:layout_width="@dimen/basic_item_image_size"
        android:layout_height="@dimen/basic_item_image_size"
        android:id="@+id/iv_base_icon"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:src="@mipmap/ic_launcher"
        android:layout_marginLeft="@dimen/padding_base2x"
        android:padding="2dp"
        android:background="@drawable/circle"/>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/iv_base_icon"
        android:layout_toEndOf="@+id/iv_base_icon"
        android:paddingStart="8dp"
        android:paddingLeft="8dp"
        android:paddingRight="@dimen/activity_horizontal_margin">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/App.Text.Headline"
            android:id="@+id/tv_name"
            android:textColor="@android:color/white"
            tools:text="Hrad Stara Lubovna" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/App.Text.Subheading"
            android:id="@+id/tv_place"
            android:textColor="@android:color/white"
            tools:text="Stara Lubovna" />

    </LinearLayout>
</RelativeLayout>